import { NextResponse } from "next/server";

// Phase 2 §6.1 — CV PDF + JD text in, gap analysis + roadmap + stages out.
export async function POST() {
  return NextResponse.json({ error: "not_implemented" }, { status: 501 });
}
