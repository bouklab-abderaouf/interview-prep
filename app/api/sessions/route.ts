import { NextResponse } from "next/server";

// Phase 3 §7.1 — create a session row when a stage interview starts.
export async function POST() {
  return NextResponse.json({ error: "not_implemented" }, { status: 501 });
}
